<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="CustomToolbarTheme" parent="Base.ThemeOverlay.AppCompat.ActionBar">
        <item name="android:textColorPrimary">@color/light_grey</item>
        <item name="actionMenuTextColor">@color/light_grey</item>
        <item name="android:actionMenuTextColor">@color/light_grey</item>
    </style>

    <style name="MyActionBar">
        <item name="android:textColorPrimary">@android:color/black</item>
        <item name="colorPrimary">@android:color/transparent</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="windowActionBarOverlay">true</item>
    </style>

    <style name="ColorSwitchStyle">
        <item name="colorControlActivated">@color/white</item>
        <item name="colorAccent">@color/white</item>
        <item name="colorControlHighlight">@color/white</item>
    </style>

    <style name="EditTextStyle">
        <item name="colorAccent">@color/white</item>

    </style>

    <style name="FilterButtonStyle" parent="Theme.AppCompat.Light">
        <!--ボタンの色-->
        <item name="colorButtonNormal">#FAFAFA</item>
        <!--ボタンをプレスしている時、シークバーをプレスしている時の色-->
        <item name="colorControlHighlight">@color/inactive_gray</item>
    </style>

    <style name="RequestButtonStyle" parent="Theme.AppCompat">
        <!--ボタンの色-->
        <item name="colorButtonNormal">@color/light_blue</item>
        <!--ボタンをプレスしている時、シークバーをプレスしている時の色-->
        <item name="colorControlHighlight">@color/light_blue2</item>
    </style>
</resources>
